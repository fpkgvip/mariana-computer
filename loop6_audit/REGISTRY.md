# Loop 6 — Bug Registry (Phase B consolidation)

Generated by Phase B from: A1_db.md (17), A2_api.md (8), A3_orchestrator.md (8), A4_frontend.md (12), A5_adversarial.md (10).
Total lens findings: 55.  Total canonicals after dedup: 46.

---

## Section 1: Dedup Table

Every YAML finding appears under exactly one canonical.  Merge rules from the task brief applied verbatim.

| Canonical | Lens IDs merged | Severity | Surface | Title |
|-----------|----------------|----------|---------|-------|
| B-01 | A1-01, A1-03, A1-04, A5-01, A5-03 | P0 | db | Anon-callable SECURITY DEFINER RPCs — credit inflation/drain, IDOR, role escalation via JSONB merge | **FIXED 2026-04-27** (mig 005 split-revoke + api.py service_role) |
| B-02 | A1-02, A1-17, A5-04 | P1 | db | SECURITY DEFINER functions missing SET search_path — schema-shadowing attack vector | **FIXED 2026-04-27** (mig 007 SET search_path on 10 SECURITY DEFINER fns) |
| B-03 | A2-01 | P1 | api | Stripe webhook marks event processed before business logic succeeds — lost credit grants on retry | **FIXED 2026-04-27** (two-phase claim/finalize + 8 regression tests) |
| B-04 | A2-02 | P1 | api | Stripe refund and dispute events never reverse previously granted credits | **FIXED 2026-04-27** (refund_credits RPC corrected to debit; charge.refunded/dispute handlers added; 10 regression tests) |
| B-05 | A1-08, A2-03 | P1 | cross | add_credits / deduct_credits bypass credit_buckets/credit_transactions ledger — R6 drift | **FIXED 2026-04-27** (mig 007 grant/spend/refund/expire sync profiles.tokens; one-shot backfill on live) |
| B-06 | A5-02 | P1 | db | admin_set_credits absolute write races concurrent spend — last-writer-wins, audit dirty read | **FIXED 2026-04-27** (mig 007 admin_set_credits SELECT...FOR UPDATE) |
| B-07 | A1-09, A5-10 | P1 | db | spend_credits no SELECT FOR UPDATE — two-tab concurrent spend underflows; balance_after racy | **FIXED 2026-04-27** (verified pg_advisory_xact_lock already in place; mig 007 also adds profiles.tokens sync) |
| B-08 | A4-02 | P1 | frontend | Navbar / BuyCredits show stale profiles.tokens — never auto-refreshes after spend or webhook | **FIXED 2026-04-27** (Navbar.tsx + BuyCredits.tsx use useCredits() hook; hook extended with focus/visibilitychange/30s poll; 7 vitest tests added) |
| B-09 | A4-03 | P1 | frontend | Full JWT access token exposed in SSE query string when stream-token mint fails or is absent | **FIXED 2026-04-27** (stream-token mint enforced in Chat.tsx + AgentTaskView.tsx + agentRunApi.ts; fallback-to-JWT removed; 30 tests added across TS and Python) |
| B-10 | A4-04 | P1 | frontend | No Content-Security-Policy or any security header in vercel.json | **FIXED 2026-04-27** (vercel.json headers + vitest contract) |
| B-11 | A1-05 | P2 | db | admin_count_profiles / admin_list_profiles use inline auth.uid() check instead of is_admin + missing search_path | **FIXED 2026-04-27** (mig 011: rewrote both functions to call public.is_admin(auth.uid()); SET search_path = public, pg_temp; C10 contract added) |
| B-12 | A1-06, A5-06 | P2 | db | admin_audit_insert publicly executable — anonymous / any-user forge or pollute audit log | **FIXED 2026-04-27** (mig 011: REVOKE EXECUTE FROM anon, authenticated; GRANT to service_role only; body checks auth.uid() == p_actor_id; C11 contract added) |
| B-13 | A1-07, A5-07 | P2 | db | expire_credits callable by anon — DoS by repeated full-table credit sweep | **FIXED 2026-04-27** (mig 011: REVOKE EXECUTE FROM anon, authenticated; GRANT to service_role only; idempotent; C12 contract added) |
| B-14 | A5-08 | P2 | db | handle_new_user trigger failure leaves phantom auth.users without profiles row | **FIXED 2026-04-27** (mig 011: EXCEPTION handler wraps body; ON CONFLICT (id) DO NOTHING on profiles INSERT; credit_buckets INSERT in nested sub-transaction; re-raises to roll back auth.users; SECURITY DEFINER + search_path; C13 contract added) |
| B-15 | A1-10 | P2 | db | credit_buckets / credit_transactions FKs reference auth.users not profiles — CASCADE DELETE inconsistency | **FIXED 2026-04-27** (mig 011: dropped FK to auth.users; added FK to public.profiles(id) ON DELETE CASCADE on both tables; orphan pre-check aborts migration if orphans found; C14 contract added) |
| B-16 | A1-11 | P2 | db | admin_set_credits updates profiles.tokens but skips credit_buckets ledger — R3 admin path | **FIXED 2026-04-27** (mig 012: admin_set_credits now inserts credit_buckets[source='admin_grant'] + credit_transactions[type='grant'/'spend'] rows; approach (a) direct set path; B-05 invariant preserved; applied local+live; C15 contract + 5 pytest tests) |
| B-17 | A2-04 | P2 | api | Legacy admin credits endpoint keeps direct-token mutation path alive alongside newer v2 endpoint | **FIXED 2026-04-27** (api.py: v1 endpoint /api/admin/users/{uid}/credits now delegates to admin_adjust_credits via _admin_rpc_call instead of direct admin_set_credits httpx call; 4 regression tests) |
| B-18 | A2-05 | P2 | api | Multiple admin mutation routes silently ignore audit-log failures after state already changed | **FIXED 2026-04-27** (api.py: _audit_or_503 helper added; all 4 silent except HTTPException: pass audit swallows replaced; admin_feature_flags_upsert/delete + admin_danger_flush_redis/halt_running now return 503 on audit failure; 7 regression tests) |
| B-19 | A2-06 | P2 | api | Shutdown route bypasses JWT admin check — controlled only by shared header secret | **FIXED 2026-04-27** (api.py: graceful_shutdown now requires _require_admin dependency (Supabase JWT + is_admin check) in addition to X-Admin-Key header; 5 regression tests) |
| B-20 | A2-07 | P2 | api | Admin authorization cache holds stale positive decisions up to 30 s after role revocation | **FIXED 2026-04-27** (api.py: positive decisions no longer cached; negative results cached 5s max; _clear_admin_cache() helper added; admin_user_set_role calls _clear_admin_cache on role change; 6 regression tests) |
| B-21 | A5-05 | P2 | api | In-process rate limiter not shared across workers/instances — bypassable by horizontal fan-out | **FIXED 2026-04-27** (api.py: slowapi Limiter constructed with storage_uri=REDIS_URL when configured; falls back to per-process with RuntimeWarning when REDIS_URL absent; 6 regression tests) |
| B-22 | A3-03 | P2 | orchestrator | Atomic credit probe refund uses wrong RPC parameter names — 1 credit lost per probe call | **FIXED 2026-05-13** (event_loop.py: add_credits call changed from {target_user_id, amount} to {p_user_id, p_credits} matching live DB signature; 6 regression tests in test_b22_probe_refund_params.py) |
| B-23 | A3-01 | P2 | orchestrator | branch_manager imports non-existent get_config — all AppConfig thresholds silently use hardcoded defaults | **FIXED 2026-05-13** (branch_manager.py: _cfg_val replaced with direct os.environ.get; import os added; 6 regression tests in test_b23_branch_manager_config.py) |
| B-24 | A3-02 | P2 | orchestrator | Module-level _metadata_lock serializes all concurrent investigations on a single asyncio.Lock | **FIXED 2026-05-13** (event_loop.py: _metadata_lock replaced with _metadata_locks dict + _get_metadata_lock/_cleanup_metadata_lock helpers; both handle_tribunal and handle_skeptic use per-task lock; cleanup in run() finally; 6 regression tests in test_b24_metadata_lock.py) |
| B-25 | A4-01 | P2 | frontend | Billing portal redirect trusts server URL without validation — open redirect to arbitrary domain | **FIXED 2026-05-12** (Account.tsx URL allow-list guard mirroring Checkout.tsx; 12 regression tests in b25_billing_portal_redirect.test.ts) |
| B-26 | A4-05 | P2 | frontend | FileViewer hand-rolled markdown renderer diverges from Chat.tsx — no link href sanitization guard | **FIXED 2026-05-12** (FileViewer.tsx link transform added with https?:// allow-list; javascript:/data:/vbscript:/file: stripped to plain text; 10 regression tests in b26_fileviewer_markdown_xss.test.ts) |
| B-27 | A4-06 | P2 | frontend | PreviewPane iframe uses allow-same-origin — user-generated app shares app origin | **FIXED 2026-05-12** (PreviewPane.tsx sandbox attribute — allow-same-origin removed, leaving allow-scripts allow-forms allow-modals allow-popups allow-downloads; 7 tests in b27_preview_pane_sandbox.test.ts) |
| B-28 | A4-07 | P2 | frontend | AuthContext loading spinner has no timeout — infinite spinner on Supabase outage | **FIXED 2026-05-12** (AuthContext.tsx: 10 s timeout via AUTH_LOADING_TIMEOUT_MS, authTimedOut state, recoverable Retry UI with data-testid="auth-timeout", accessible loading spinner; 15 tests in b28_auth_loading_timeout.test.ts) |
| B-29 | A4-08 | P2 | frontend | success_url checkout landing pages never call refreshUser — credit balance stale post-payment | **FIXED 2026-05-12** (Chat.tsx, Build.tsx, Account.tsx: detect ?checkout=success/?topup=success on mount, show success toast, poll refreshUser/refetchBalance up to 3×, clear URL param; 20 tests in b29_post_checkout_credit_refresh.test.ts) |
| B-30 | A5-09 | P3 | api | Webhook secret rotation has no overlap window — in-flight events dropped during rotation | **FIXED 2026-05-14** (api.py + config.py: dual-secret PRIMARY/PREVIOUS rotation; 7 tests in test_b30_webhook_dual_secret.py) |
| B-31 | A2-08 | P3 | api | Billing usage endpoint always falls back to free-plan because auth context lacks subscription fields | **FIXED 2026-05-14** (api.py: _supabase_get_subscription_fields helper + billing_usage reads profiles; 6 tests in test_b31_billing_usage_plan.py) |
| B-32 | A1-12 | P3 | db | 7 FK columns missing covering indexes — cascade-delete and JOIN scans sequential | **FIXED 2026-04-27** (mig 013_p3_b32_fk_indexes.sql: 7 idx_* covering indexes; C16 contract; applied to NestD live) |
| B-33 | A1-13 | P3 | db | 20+ RLS policies use bare auth.uid() / auth.role() — re-evaluated per row, planning overhead | **FIXED 2026-04-27** (mig 014_p3_b33_rls_select_wrap.sql: wrapped 20+ policies with (SELECT auth.uid()); C17 contract; applied to NestD live) |
| B-34 | A1-14 | P3 | db | profiles_owner_update_safe WITH CHECK has 9 correlated subqueries per UPDATE — O(n) self-joins | **FIXED 2026-04-27** (mig 015_p3_b34_profile_check_simplify.sql: check_profile_immutable() helper, single index scan; applied to NestD live) |
| B-35 | A1-15 | P3 | db | property-images storage bucket allows unauthenticated file listing | **FIXED 2026-04-27** (mig 016_p3_b35_storage_rls.sql: dropped broad anon policy, added authenticated-only SELECT/INSERT; C18 contract; applied to NestD live) |
| B-36 | A1-16 | P3 | docs | Migration drift — 001_initial_schema.sql retains superseded "Users can update own profile" policy | **FIXED 2026-04-27** (build_local_baseline_v2.sh drops stale policy; 001_initial_schema.sql annotated; baseline rebuild green) |
| B-37 | A3-04 | P3 | orchestrator | FRED connector includes api_key in cache key — cross-deployment cache miss when key differs | **FIXED 2026-05-14** (fred_connector.py: api_key excluded from cache_params; 6 tests in test_b37_fred_cache_key.py) |
| B-38 | A3-05 | P3 | orchestrator | URL content cache is global (no task/user isolation) — stale financial data served cross-investigation | **FIXED 2026-05-14** (cache.py: task_id param added to _url_cache_key, get_url, set_url, delete_url, exists; 7 tests in test_b38_url_cache_task_isolation.py) |
| B-39 | A3-06 | P3 | orchestrator | Vault KDF allows client-supplied kdf_iterations=1 — below OWASP minimum | **FIXED 2026-05-14** (vault/router.py: kdf_iterations ge raised 1→2; server-side 400 guard; 6 tests in test_b39_vault_kdf_minimum.py) |
| B-40 | A3-07 | P3 | orchestrator | Browser pool server has no authentication on /dispatch or /pool/status | **FIXED 2026-05-14** (pool_server.py: _require_pool_auth dependency on /dispatch + /pool/status via BROWSER_POOL_SECRET; 8 tests in test_b40_browser_pool_auth.py) |
| B-41 | A3-08 | P3 | orchestrator | DB total_spent_usd stores raw model cost, not 1.20× markup — 20% reconciliation gap | **FIXED 2026-05-14** (_sync_cost now persists total_spent × 1.20 via _COST_MARKUP_MULTIPLIER; 6 tests in test_b41_cost_markup.py) |
| B-42 | A4-09 | P3 | frontend | Supabase JWT stored in localStorage — exfiltrable by any XSS on same origin | **FIXED 2026-04-27** (sessionStorage adapter in supabase.ts; ADR-B42; 12 regression tests) |
| B-43 | A4-10 | P3 | frontend | vercel.json backend rewrite targets bare IP over plain HTTP — no TLS on backend hop | **FIXED 2026-04-27** (rewrites updated http://77.42.3.206:8080 → https://api.deft.computer; 8 regression tests) |
| B-44 | A4-11 | P3 | frontend | jsdom pinned at ^20.0.3 — known XSS-bypass CVEs; should be ≥24.x | **FIXED 2026-04-27** (bumped to ^24.0.0; resolved 24.1.3; 6 regression tests) |
| B-45 | A5-01 sub: get_stripe_customer_id IDOR | — | — | (folded into B-01; listed for completeness) |
| B-46 | A4-12 | P4 | frontend | AuthProvider loading spinner has no ARIA role — screen readers see nothing during auth init | **FIXED-by-B28 2026-04-27** (B-28 added role="status", aria-live="polite", sr-only text; verified by b28_auth_loading_timeout.test.ts tests 9–10) |
| F-01 | Phase E | P1 | api | Public preview route /preview/{task_id}/{file_path} bypasses owner check — IDOR on user previews | **FIXED 2026-04-27** (HMAC preview tokens via cookie+query+header; manifest mints HttpOnly cookie scoped to /preview/{task_id}; 15 regression tests) |
| F-02 | Phase E | P1 | cross | start_investigation does not hold pending-upload lock during file move — double-charge race on shared upload session | **FIXED 2026-04-27** (api.py: pending-{session_uuid} lock + atomic os.rename to claimed/, 409 + credit refund on conflict; 6 regression tests) |
| F-03 | Phase E | P1 | cross | Refund/dispute clawback clamped to current balance — already-spent credits forgiven, no debt construct | **FIXED 2026-04-27** (mig 009: credit_clawbacks table + refund_credits records deficit + grant_credits/add_credits drain open clawbacks FIFO; 8 pytest + C08 contract; applied to live) |
| F-04 | Phase E | P1 | cross | Subscription downgrade/cancel updates subscription_plan only; profiles.plan (used for entitlement enforcement) is never updated | **FIXED 2026-04-27** (mig 008: update_profile_by_stripe_customer syncs plan + reconcile pass; api.py webhooks include effective_plan; 9 regression tests; applied to live, 12 stale flagship profiles reconciled to free) |
| F-05 | Phase E | P2 | cross | research_tasks owner stored in metadata JSONB only — deleting auth.users orphans tasks and descendants | **FIXED 2026-04-27** (mig 010: research_tasks.user_id NOT NULL + ON DELETE CASCADE; runtime-table guard for init_schema-created table; backfill from metadata; ownership checks updated; tests + C09 contract green) |
| F-06 | Phase E | P3 | api | Intelligence endpoints return unbounded per-task datasets to the browser — no pagination/limits | **FIXED 2026-04-27** (api.py: keyset cursor + server-side cap on /api/intelligence/* endpoints; tests green) |
| G-01 | Phase E re-audit #2 | P1 | api | _get_upload_lock used WeakValueDictionary — locks GC-eligible immediately, breaking F-02 race fix and upload file-cap | **FIXED 2026-04-27** (api.py: strong-reference OrderedDict-based LRU bounded at 4096 entries, evicts only unheld locks; 5 regression tests in test_g01_upload_lock_strong_ref.py) |
| H-01 | Phase E re-audit #3 | P1 | billing/webhooks | Refund/dispute lookup falls back to most recent unrelated Stripe grant when pi_id metadata absent — cross-account credit theft | **FIXED 2026-04-27** (mig 017: stripe_payment_grants explicit pi linkage; _lookup_grant_tx_for_payment_intent queries only stripe_payment_grants; global latest-grant fallback removed; 6 regression tests) |
| H-02 | Phase E re-audit #3 | P2 | billing/webhooks | Both charge.dispute.created and charge.dispute.funds_withdrawn trigger reversal with only event_id dedup — double clawback per dispute | **FIXED 2026-04-27** (mig 017: stripe_dispute_reversals dedup by reversal_key=dispute:<id> or charge:<id>:reversal; _reverse_credits_for_charge checks/records dedup row; 5 regression tests) |
| I-01 | Phase E re-audit #4 | P2 | db/billing | add_credits SQL function lacks pg_advisory_xact_lock(hash(user_id)) used by sibling ledger functions — race with refund_credits double-credits user when clawback row commits between v_open_total read and FOR UPDATE loop | **FIXED 2026-04-27** (mig 018: pg_advisory_xact_lock added as first statement after input validation; matches refund_credits/grant_credits; 5 regression tests) |
| I-02 | Phase E re-audit #4 | P2 | billing/webhooks | _record_dispute_reversal_or_skip is check-then-insert TOCTOU — concurrent dispute.created and dispute.funds_withdrawn webhooks can both pass SELECT and double-debit before INSERT collapses on PK | **FIXED 2026-04-27** (api.py: _compute_reversal_key helper + _refund_rpc ref_id=reversal_key; stable key across all dispute event types; RPC-level dedup collapses concurrent calls; 6 regression tests) |
| I-03 | Phase E re-audit #4 | P3 | db | loop6_007_applied + loop6_008_applied marker tables in public schema have RLS disabled and grant ALL to anon/authenticated — unauth tampering surface | **FIXED 2026-04-27** (mig 019: ALTER TABLE ENABLE RLS + REVOKE ALL FROM anon/authenticated; C20 contract; d82b7f6) |
| J-01 | Phase E re-audit #5 | P2 | billing/webhooks | _handle_charge_refunded uses single charge-scoped reversal_key for all sequential partial refunds — cumulative amount_refunded grows but second/third refund events collapse as duplicates, under-clawback | **FIXED 2026-04-27** (api.py: _compute_reversal_key uses refund_event:<event_id> for charge.refunded; _reverse_credits_for_charge subtracts already_reversed via _sum_reversed_credits_for_charge; 5 regression tests in test_j01_partial_refund_sequence.py) |
| J-02 | Phase E re-audit #5 | P2 | billing/webhooks | Dispute after partial refund passes dispute.amount as full charge_obj amount_refunded — _reverse_credits_for_charge then debits full original credits, double-counting the prior partial-refund debit, over-clawback | **FIXED 2026-04-27** (api.py: same incremental-debit primitive applied cross-category — dispute subtracts prior refund reversals on same charge_id; 4 regression tests in test_j02_dispute_after_refund.py) |
| K-01 | Phase E re-audit #6 | P2 | billing/webhooks | _handle_charge_dispute_* builds pseudo-charge with amount=amount_refunded=dispute.amount — partial-amount disputes (dispute.amount<charge.amount) collapse into the full-reversal else branch and over-debit the entire grant | **FIXED 2026-04-28** (mig 020: stripe_payment_grants.charge_amount column persists original Stripe amount; api.py captures charge_amount on grant and uses grant_tx['charge_amount'] as amount_total in dispute reversal so partial disputes compute correct pro-rata; 5 regression tests in test_k01_partial_dispute.py) |
| K-02 | Phase E re-audit #6 | P1 | billing/webhooks | TOCTOU between concurrent webhook handlers: _sum_reversed_credits_for_charge runs before _insert_dispute_reversal; two distinct event_ids on same charge both observe already_reversed=N and double-debit with non-overlapping increments. RPC ref_id idempotency does not collapse them because keys differ. | **FIXED 2026-04-28** (mig 021: process_charge_reversal SECURITY DEFINER RPC takes per-charge pg_advisory_xact_lock then performs dedup-check, sum-by-charge, refund_credits, and stripe_dispute_reversals INSERT atomically; api.py replaces client-side SELECT/sum/refund/insert chain with single RPC POST; 4 regression tests in test_k02_concurrent_webhook_race.py) |

> **Note on B-45:** get_stripe_customer_id IDOR is fully subsumed by B-01 (same anon-callable SECURITY DEFINER family). No separate canonical required.

---

## Section 2: Execution Order DAG

DB foundational fixes (REVOKE grants, search_path hardening, row locks) precede API and frontend fixes that depend on them.  Within a tier, items with no `blocked_by` come first.

| Order | Canonical | Severity | fix_type | blocks | blocked_by | applies_to |
|-------|-----------|----------|----------|--------|------------|------------|
| 1 | B-01 | P0 | migration | B-02, B-05, B-06, B-07, B-12, B-13, B-16, B-21 | none | db | **FIXED** |
| 2 | B-10 | P1 | frontend | none | none | frontend | **FIXED** |
| 3 | B-03 | P1 | api_patch | B-04 | none | api.py | **FIXED** |
| 4 | B-09 | P1 | config | none | none | frontend | **FIXED** |
| 5 | B-02 | P1 | migration | B-14 | B-01 | db | **FIXED** |
| 6 | B-04 | P1 | api_patch | none | B-03 | api.py | **FIXED** |
| 7 | B-05 | P1 | api_patch | B-16, B-17 | B-01 | db + api.py | **FIXED** |
| 8 | B-06 | P1 | migration | none | B-01 | db | **FIXED** |
| 9 | B-07 | P1 | migration | B-22 | B-01 | db | **FIXED** |
| 10 | B-08 | P1 | frontend_patch | none | none | frontend | **FIXED** |
| 11 | B-11 | P2 | migration | none | none | db | **FIXED** (mig 011) |
| 12 | B-12 | P2 | migration | none | B-01 | db | **FIXED** (mig 011) |
| 13 | B-13 | P2 | migration | none | B-01 | db | **FIXED** (mig 011) |
| 14 | B-14 | P2 | migration | none | B-02 | db | **FIXED** (mig 011) |
| 15 | B-15 | P2 | migration | none | none | db | **FIXED** (mig 011) |
| 16 | B-16 | P2 | migration | B-17 | B-01, B-05 | db | **FIXED** |
| 17 | B-17 | P2 | api_patch | none | B-05, B-16 | api.py | **FIXED** |
| 18 | B-18 | P2 | api_patch | none | none | api.py | **FIXED** |
| 19 | B-19 | P2 | api_patch | none | none | api.py | **FIXED** |
| 20 | B-20 | P2 | api_patch | none | none | api.py | **FIXED** |
| 21 | B-21 | P2 | api_patch | none | B-01 | api.py | **FIXED** |
| 22 | B-22 | P2 | api_patch | none | B-07 | api.py (orchestrator) | **FIXED 2026-05-13** |
| 23 | B-23 | P2 | api_patch | none | none | api.py (orchestrator) | **FIXED 2026-05-13** |
| 24 | B-24 | P2 | api_patch | none | none | api.py (orchestrator) | **FIXED 2026-05-13** |
| 25 | B-25 | P2 | frontend_patch | none | none | frontend | **FIXED 2026-05-12** |
| 26 | B-26 | P2 | frontend_patch | none | none | frontend | **FIXED 2026-05-12** |
| 27 | B-27 | P2 | config | none | none | frontend + config | **FIXED 2026-05-12** |
| 28 | B-28 | P2 | frontend_patch | none | none | frontend | **FIXED 2026-05-12** |
| 29 | B-29 | P2 | frontend_patch | none | none | frontend | **FIXED 2026-05-12** |
| 30 | B-30 | P3 | api_patch | none | none | api.py | **FIXED 2026-05-14** |
| 31 | B-31 | P3 | api_patch | none | none | api.py | **FIXED 2026-05-14** |
| 32 | B-32 | P3 | migration | none | none | db | **FIXED 2026-04-27** (mig 013) |
| 33 | B-33 | P3 | migration | none | none | db | **FIXED 2026-04-27** (mig 014) |
| 34 | B-34 | P3 | migration | none | none | db | **FIXED 2026-04-27** (mig 015) |
| 35 | B-35 | P3 | migration | none | none | db | **FIXED 2026-04-27** (mig 016) |
| 36 | B-36 | P3 | docs | none | none | docs | **FIXED 2026-04-27** (build_local_baseline_v2.sh) |
| 37 | B-37 | P3 | api_patch | none | none | api.py (orchestrator) | **FIXED 2026-05-14** |
| 38 | B-38 | P3 | api_patch | none | none | api.py (orchestrator) | **FIXED 2026-05-14** |
| 39 | B-39 | P3 | api_patch | none | none | api.py (orchestrator) | **FIXED 2026-05-14** |
| 40 | B-40 | P3 | api_patch | none | none | api.py (orchestrator) | **FIXED 2026-05-14** |
| 41 | B-41 | P3 | api_patch | none | none | api.py (orchestrator) | **FIXED 2026-05-14** |
| 42 | B-42 | P3 | config | none | B-09, B-27 | frontend | **FIXED 2026-04-27** |
| 43 | B-43 | P3 | config | none | none | config | **FIXED 2026-04-27** |
| 44 | B-44 | P3 | frontend_patch | none | none | frontend | **FIXED 2026-04-27** |
| 45 | B-46 | P4 | frontend_patch | none | none | frontend | **FIXED-by-B28 2026-04-27** |

---

## Section 3: Per-Canonical Detail Blocks

---

### B-01 — Anon-callable SECURITY DEFINER RPCs: credit inflation/drain, IDOR, role escalation

- **Status:** FIXED 2026-04-27 — migration `005_loop6_b01_revoke_anon_rpcs.sql` applied to NestD live (`afnbtbeayfkwznhzafay`) with split-revoke posture; `api.py` `_admin_supabase_headers` + 3 inline admin endpoints switched from `SUPABASE_ANON_KEY` to `_supabase_api_key` (prefers service_role). Group A (12 backend-only RPCs) revoked from PUBLIC/anon/authenticated; Group B (5 admin-gated RPCs with inline `is_admin()` or `role='admin'` check) revoked from PUBLIC/anon, retained for `authenticated` so the user JWT path works. Tests: contract C07 GREEN (split posture + admin gate detection), full suite GREEN (7 contracts + 18 pytest + 15 vitest). Live grants verified: Group A = postgres+service_role only; Group B = postgres+service_role+authenticated.
- **Severity:** P0
- **Surface:** db
- **Lens findings merged:** A1-01, A1-03, A1-04, A5-01, A5-03
- **Root cause:** `add_credits`, `deduct_credits`, `get_user_tokens`, `check_balance`, `get_stripe_customer_id`, `update_profile_by_id`, `update_profile_by_stripe_customer` are all `SECURITY DEFINER` with `EXECUTE` granted to `PUBLIC` and `anon`. None contains a caller-identity check. `update_profile_by_id` accepts a free-form JSONB payload and writes `role`, `tokens`, and `subscription_status` columns directly — any anon caller can self-promote to admin and set arbitrary balances.
- **Fix sketch:**
  - New migration: `REVOKE EXECUTE ON FUNCTION public.add_credits, public.deduct_credits, public.get_user_tokens, public.check_balance, public.get_stripe_customer_id, public.update_profile_by_id, public.update_profile_by_stripe_customer FROM PUBLIC, anon, authenticated;`
  - Rewrite each function to begin: `IF auth.uid() IS NULL OR (auth.uid() <> p_user_id AND NOT public.is_admin(auth.uid())) THEN RAISE EXCEPTION 'permission denied' USING ERRCODE = '42501'; END IF;`
  - For `update_profile_by_id` / `update_profile_by_stripe_customer`: remove JSONB-merge pattern; replace with explicit column params that exclude `role`, `tokens`, `id`, `created_at`.
  - Add `SET search_path = ''` and fully qualify all table references as `public.<table>` inside each function body.
  - `api.py` callers must use the Supabase service-role key (not anon key) for these RPCs. Files: `mariana/api.py` (all `_rpc("add_credits", ...)` / `_rpc("deduct_credits", ...)` call sites).
  - Evidence files: `pg_catalog / information_schema.role_routine_grants` (A1-01 lines: `prosecdef=true, proconfig=null, ACL includes =X/postgres (PUBLIC) and anon=X/postgres`); `pg_catalog` (A5-01 query result table showing anon=t for all 7 RPCs).
- **Test to add:** `tests/contracts/C07_anon_rpc_deny.py` — for each of the 7 RPCs, run as anon with a non-self UUID, expect SQLSTATE 42501; run as authenticated non-admin with non-self UUID, expect 42501; run `update_profile_by_id` with `{"role":"admin"}`, expect 42501 or no-op on role column.
- **Blocking dependencies:** none
- **Confidence:** high
- **Notes:** A5-03 is a sub-case of the same family — `update_profile_by_id` is the escalation path once anon access is established. All five lens findings share the same migration. A3-03 (wrong probe param names in orchestrator, B-22) compounds this finding but is a separate fix. BUG-04 and BUG-11 in A3's verification table are FALSE POSITIVES (retracted) and do not affect this canonical.

---

### B-02 — SECURITY DEFINER functions missing SET search_path

- **Severity:** P1
- **Surface:** db
- **Lens findings merged:** A1-02, A1-17, A5-04
- **Root cause:** 10 SECURITY DEFINER functions have `proconfig IS NULL` (no `SET search_path`). The full list per A1-02: `add_credits`, `admin_count_profiles`, `admin_list_profiles`, `check_balance`, `deduct_credits`, `get_stripe_customer_id`, `get_user_tokens`, `handle_new_user`, `update_profile_by_id`, `update_profile_by_stripe_customer`. Because `pg_temp` is prepended to the search path by default, a caller can `CREATE TEMP TABLE profiles (...)` with a poisoned schema and shadow the real table during function execution. `handle_new_user` (A1-17) fires on signup and is particularly sensitive.
- **Fix sketch:**
  - For every SECURITY DEFINER function: add `SET search_path = ''` to the `CREATE OR REPLACE FUNCTION` declaration.
  - Fully qualify every table and function reference inside the body: `public.profiles`, `public.credit_buckets`, `auth.uid()`, etc.
  - File: new migration in `frontend/supabase/migrations/005_search_path_hardening.sql`.
  - Evidence: `pg_catalog` query `SELECT proname, proconfig FROM pg_proc ... WHERE prosecdef=true` returns `proconfig IS NULL` for all 10 listed functions (A1-02 lines: `proconfig IS NULL for: add_credits, admin_count_profiles, ...`).
- **Test to add:** `tests/contracts/C10_search_path_isolation.py` — for each definer function, create a TEMP TABLE shadow with the same name, invoke the function, assert the real `public` table is unchanged.
- **Blocking dependencies:** B-01 (REVOKE grants before hardening; the two migrations can be batched in the same file)
- **Confidence:** high
- **Notes:** A5-04 provides the adversarial reproduction confirming the attack is real, not hypothetical. A1-17 is specifically for `handle_new_user`; included here because it shares the same fix (add `SET search_path = ''`). B-14 (handle_new_user trigger idempotency) depends on this landing first. Can be batched in one migration with B-01's REVOKE block.

---

### B-03 — Stripe webhook marks event processed before business logic succeeds

- **Status:** FIXED 2026-04-27 — schema upgrade in `mariana/data/db.py`: `stripe_webhook_events` now carries `status` (pending|completed), `attempts`, `received_at`, `last_attempt_at`, `completed_at`, `last_error` columns (idempotent ALTER block for existing deployments). Replaced single-shot `_record_webhook_event_once` with two-phase `_claim_webhook_event` (NEW|RETRY|DUPLICATE) + `_finalize_webhook_event`. Webhook handler claims pending before business logic, finalizes only after handler returns; on raise, row stays pending and Stripe retries trigger RETRY claim. Per-grant idempotency via `uq_credit_tx_idem` (`ref_id=event_id`) prevents double-credit on retry. Backwards-compat shim retains old `_record_webhook_event_once`. New regression suite `tests/test_b03_webhook_two_phase.py` (8 tests covering NEW/RETRY/DUPLICATE/finalize-fail). Full suite: 7 SQL contracts + 66 pytest + 24 vitest GREEN.
- **Severity:** P1
- **Surface:** api
- **Lens findings merged:** A2-01
- **Root cause:** In `api.py` lines 5262–5273, `_record_webhook_event_once(evt_id, type)` is called and commits a `webhook_events` row before any credit-granting or subscription-update logic runs. If the subsequent handler logic raises an exception (DB error, network timeout), Stripe retries with the same `event_id`, but the idempotency guard rejects it as already processed — the business effect is permanently lost.
- **Fix sketch:**
  - Move `_record_webhook_event_once` to **after** the business-logic block, within the same transaction, or use a two-phase approach: mark as `pending` before, `complete` after.
  - File: `/home/user/workspace/mariana/mariana/api.py` lines 5262–5273, 5301–5317, 6001–6011.
  - Alternatively, wrap the entire handler in a transaction: `async with db.transaction(): <business logic> <record event>`.
- **Test to add:** `test_stripe_webhook_retry_after_handler_failure_reprocesses_event` — fail after `_record_webhook_event_once`, verify the next Stripe retry call succeeds and grants credits.
- **Blocking dependencies:** none
- **Confidence:** high
- **Notes:** B-04 (refund reversal) must land after this fix because it depends on correct idempotency handling.

---

### B-04 — Stripe refund and dispute events never reverse previously granted credits

- **Status:** FIXED 2026-04-27 — Migration 006 replaces the `refund_credits` SQL RPC with a FIFO-debit implementation (type='refund' transactions). Three new handler functions (`_handle_charge_refunded`, `_handle_charge_dispute_created`, `_handle_charge_dispute_funds_withdrawn`) added to `mariana/api.py`. All three wired into the webhook dispatch block (lines ~5308-5319). Pro-rata partial refund logic included. Idempotency via `uq_credit_tx_idem` partial unique index on `(ref_type, ref_id)` for type='refund'. 10 regression tests in `tests/test_b04_refund_dispute.py`. Full suite GREEN.
- **Severity:** P1
- **Surface:** api
- **Lens findings merged:** A2-02
- **Root cause:** The webhook handler in `api.py` lines 5277–5299 routes `charge.refunded` and `charge.dispute.created` events to a no-op or logging path. `billing/ledger.py` lines 152–173 contain no `reverse_grant` or credit-deduction call. Users who receive refunds or lose disputes retain their originally granted credits.
- **Fix sketch:**
  - In `mariana/billing/ledger.py`, implement `reverse_grant(charge_id: str, user_id: uuid)` that inserts a `credit_transactions` row of type `refund` with `amount = -original_grant_amount` and updates `profiles.tokens` (or calls `deduct_credits` once that is locked down per B-01/B-05).
  - In `api.py` lines 5277–5299, call `reverse_grant` inside the `charge.refunded` and `charge.dispute.created` handlers.
  - File: `/home/user/workspace/mariana/mariana/api.py` lines 5277–5299; `/home/user/workspace/mariana/mariana/billing/ledger.py` lines 152–173.
- **Test to add:** `test_stripe_refund_event_reverses_prior_grant` — grant credits via `charge.succeeded`, deliver `charge.refunded` event, assert credit balance returns to pre-grant level and ledger shows a balancing debit.
- **Blocking dependencies:** B-03 (idempotency must be fixed first so the reversal isn't skipped on retry)
- **Confidence:** high

---

### B-05 — add_credits / deduct_credits bypass credit_buckets/credit_transactions ledger (R6)

- **Severity:** P1
- **Surface:** cross (db + api)
- **Lens findings merged:** A1-08, A2-03
- **Root cause:** `add_credits` (pg_catalog prosrc) updates `profiles.tokens` directly and never inserts into `credit_buckets` or `credit_transactions`. Same for `deduct_credits`. The investigation reservation path in `api.py` lines 2677–2699 and the refund path at 2875–2912 call these RPCs, meaning every investigation start/cancel also bypasses the ledger. This is the R6 known open issue.
- **Fix sketch:**
  - Rewrite `add_credits` to: (1) `SELECT ... FOR UPDATE` on `profiles` (row-lock per B-06/B-07), (2) insert a `credit_buckets` row with appropriate `expires_at`, (3) insert a `credit_transactions` row with `type='grant'`, (4) update `profiles.tokens` to match the new ledger sum. File: new migration `005_ledger_repair.sql`.
  - Rewrite `deduct_credits` analogously against the existing `spend_credits` ledger path.
  - In `api.py` lines 2677–2699 and 2875–2912, replace direct `_rpc("add_credits", ...)` / `_rpc("deduct_credits", ...)` calls with calls to the properly ledger-writing RPC equivalents (or via `billing/ledger.py`). Evidence: `/home/user/workspace/mariana/mariana/api.py` lines 2677–2699, 5816–5867, 5906–5987.
  - Surface via `tools/reconcile_ledger.py` (existing R3 reconciler) to quantify current drift before migration.
- **Test to add:** `test_ledger_consistency_after_grant` — after `add_credits` call, verify `profiles.tokens` equals sum of `credit_buckets.balance` for that user and a `credit_transactions` row of type `grant` exists.
- **Blocking dependencies:** B-01 (REVOKE anon execute first to prevent race during migration window)
- **Confidence:** high
- **Notes:** R6 (open in `_SHARED_CONTEXT.md`) is confirmed by this canonical. R3 (`profiles.tokens` vs ledger drift) is the observable symptom; `tools/reconcile_ledger.py` exists to quantify drift. B-16 (admin_set_credits ledger bypass) and B-17 (legacy admin endpoint) share the same root cause and should be batched in the same migration where possible.

---

### B-06 — admin_set_credits absolute write races concurrent spend

- **Severity:** P1
- **Surface:** db
- **Lens findings merged:** A5-02
- **Root cause:** `admin_set_credits` reads `profiles.tokens` without `SELECT ... FOR UPDATE`, then writes the absolute new balance. A concurrent `spend_credits` that reads the old balance and commits after `admin_set_credits` silently overwrites the admin's intended value (or vice versa). The `previous_balance` captured in `admin_audit_log` is a dirty read. Evidence: pg_catalog prosrc for `admin_set_credits` + reproduction via two interleaved psql sessions (A5-02).
- **Fix sketch:**
  - Add `SELECT tokens INTO v_current FROM public.profiles WHERE id = p_user_id FOR UPDATE;` at the top of `admin_set_credits` body.
  - Insert a balancing `credit_transactions` row (`type='admin_adjust'`, `credits = new_balance - v_current`) so the ledger reflects the correction.
  - Log `previous_balance = v_current` (from the locked read, not the pre-lock snapshot) in `admin_audit_log`.
  - File: migration on `public.admin_set_credits`; batched with `005_ledger_repair.sql`.
- **Test to add:** `tests/contracts/C08_admin_spend_race.py` — interleave `spend_credits` and `admin_set_credits` via two asyncpg connections using savepoints; assert final `profiles.tokens` matches a deterministic ordering and ledger sums equal the balance.
- **Blocking dependencies:** B-01 (REVOKE grants first; admin_set_credits must be tightened before row-locking)
- **Confidence:** high
- **Notes:** Same-family as B-07 (spend race) — both require `SELECT ... FOR UPDATE`. Can be applied in the same migration. A5-02 explicitly lists `blocking: [A5-01]`.

---

### B-07 — spend_credits no SELECT FOR UPDATE — concurrent spend underflows balance

- **Severity:** P1
- **Surface:** db
- **Lens findings merged:** A1-09, A5-10
- **Root cause:** `spend_credits` body reads `profiles.tokens` into a local variable, then iterates `credit_buckets`, then writes back. There is no `SELECT ... FOR UPDATE` on the `profiles` row. Two concurrent calls both read the same `tokens=1000`, both write `tokens=400` — one spend is silently lost while the ledger records both debits (`sum(ledger.spend)=1200` vs `profiles.tokens=400`). Additionally, `balance_after` in the per-bucket loop reflects a partial mid-loop snapshot, not the final post-all-buckets balance (A1-09). Evidence: pg_catalog prosrc for `spend_credits`; local two-session reproduction in A5-10.
- **Fix sketch:**
  - Add `SELECT tokens INTO v_current FROM public.profiles WHERE id = p_user_id FOR UPDATE;` at the start of `spend_credits`.
  - `IF v_current < p_amount THEN RAISE EXCEPTION 'insufficient_credits';` before any bucket writes.
  - Compute `balance_after` once, after all bucket iterations complete, and store that single value in all `credit_transactions` rows for this call.
  - File: migration on `public.spend_credits`; batched in `005_ledger_repair.sql`.
- **Test to add:** `tests/contracts/C14_concurrent_spend.py` — fire two `spend_credits(user, 600)` in parallel via `asyncio.gather` starting from `tokens=1000`; assert exactly one succeeds, one raises `insufficient_credits`, and ledger sum equals `profiles.tokens` delta.
- **Blocking dependencies:** B-01 (anon execute revoked first)
- **Confidence:** high
- **Notes:** Same-family as B-06 (admin_set_credits race) and B-05 (ledger bypass). All three share the `005_ledger_repair.sql` migration. A5-10 lists `blocking: [A5-01]`. A1-09 `balance_after` corruption is a sub-issue; addressed in same fix.

---

### B-08 — Navbar / BuyCredits display stale profiles.tokens

- **Status:** FIXED 2026-04-27 — Replaced `user.tokens` in `Navbar.tsx` (lines 126, 203) and `BuyCredits.tsx` (line 47) with `useCredits()` hook output. Extended `useCredits.ts` to subscribe to `window focus`, `document visibilitychange`, and `deft:credits-changed` events, and to poll every 30 s as a backstop. Added 7 vitest tests in `src/__tests__/b08-useCredits.test.tsx` covering mount fetch, custom event, focus refresh, visibilitychange refresh, poll interval, and two component render assertions.
- **Severity:** P1
- **Surface:** frontend
- **Lens findings merged:** A4-02
- **Root cause:** `Navbar.tsx` lines 126 and 203 render `user.tokens` from `AuthContext`, which is set once on session sync (`AuthContext.tsx` line 84) and only updated when `refreshUser()` succeeds. `Chat.tsx` can call `refreshUser()` but failure is a silent `console.warn`. `BuyCredits.tsx` line 47 also reads the stale `user.tokens`. `Build.tsx` uses the `useCredits()` hook which hits `/api/credits/balance` and is live; Navbar does not.
- **Fix sketch:**
  - Replace `user.tokens` in `Navbar.tsx` and `BuyCredits.tsx` with the `useCredits()` hook (already used by `Build.tsx`), which subscribes to `deft:credits-changed` DOM events and hits `/api/credits/balance`.
  - File: `/home/user/workspace/mariana/frontend/src/components/Navbar.tsx` lines 126, 203; `/home/user/workspace/mariana/frontend/src/pages/BuyCredits.tsx` line 47.
- **Test to add:** `navbar-credit-stale` — render Navbar with `user.tokens=5000`; fire `deft:credits-changed` event; assert displayed credits update to the mock balance from `/api/credits/balance`, not the stale 5000.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-09 — Full JWT access token in SSE query string

- **Status:** FIXED 2026-04-27 — Chat.tsx fallback-to-JWT path removed (mint failure now shows toast.error); AgentTaskView.tsx rewired to use mintAgentStreamToken via new shared `streamAuth.ts` helper; agentRunApi.ts openAgentStream() mints before opening EventSource; new `POST /api/agent/{task_id}/stream-token` endpoint added in agent/api_routes.py; make_routes() gains optional mint_stream_token/verify_stream_token params; api.py passes those from the shared _mint/_verify functions; 12 vitest tests (streamAuth.test.ts + b09-sse-jwt.test.tsx) and 18 Python tests (test_b09_stream_token.py) all GREEN; TODO B-09-FOLLOWUP left for fetch()+ReadableStream Authorization-header path.
- **Severity:** P1
- **Surface:** frontend
- **Lens findings merged:** A4-03
- **Root cause:** `Chat.tsx` lines 1648–1698 falls back to the full Supabase JWT in the SSE URL (`/logs?token=<JWT>`) when the stream-token mint fails. `AgentTaskView.tsx` lines 83–86 always places the full JWT in the URL with no stream-token path at all. The JWT lands in nginx/CDN access logs, browser history, and Referer headers.
- **Fix sketch:**
  - Extend the stream-token mint pattern from `Chat.tsx` to `AgentTaskView.tsx` and `agentRunApi.ts openAgentStream()`.
  - Remove the fallback-to-JWT path in `Chat.tsx`: if the stream-token endpoint fails, surface an error state instead of silently downgrading.
  - Long-term: migrate EventSource to `fetch()` + `ReadableStream` with `Authorization: Bearer` header support.
  - Files: `/home/user/workspace/mariana/frontend/src/pages/Chat.tsx` lines 1648–1698; `/home/user/workspace/mariana/frontend/src/components/agent/AgentTaskView.tsx` lines 83–86; `/home/user/workspace/mariana/frontend/src/lib/agentRunApi.ts`.
- **Test to add:** `sse-no-jwt-in-url` — mock `/stream-token` to return 500; assert that neither `AgentTaskView` nor `Chat` SSE connections are established with the full JWT in the URL; assert an error state is shown.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-10 — No Content-Security-Policy or security headers in vercel.json

- **Status:** FIXED 2026-04-27 — `frontend/vercel.json` now declares a strict `headers` block applied to every route: HSTS (2yr + preload), `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` denying camera/mic/geolocation/etc, `Cross-Origin-Opener-Policy: same-origin`, `Cross-Origin-Resource-Policy: same-site`, and a tight `Content-Security-Policy` covering default/base/frame-ancestors/object/form-action/img/font/style/script/connect/frame/worker/manifest/media. CSP allows Stripe (js.stripe.com, hooks.stripe.com), Supabase (https + wss `*.supabase.co`), and the preview subdomain. `script-src` excludes `'unsafe-inline'` and `'unsafe-eval'`. Pinned by vitest contract `src/test/vercelHeaders.test.ts` (9 assertions). Full suite: 7 SQL contracts + 18 pytest + 24 vitest GREEN.
- **Severity:** P1
- **Surface:** frontend
- **Lens findings merged:** A4-04
- **Root cause:** `frontend/vercel.json` lines 1–18 contains only `buildCommand`, `outputDirectory`, `framework`, and `rewrites`. No `headers` block. No CSP, no `X-Frame-Options`, no `X-Content-Type-Options`, no HSTS, no `Permissions-Policy`. `index.html` lines 1–29 contains no `<meta http-equiv="Content-Security-Policy">`. The app uses `dangerouslySetInnerHTML` in `FileViewer.tsx` and `Chat.tsx`, and embeds user-generated previews in an iframe with `allow-same-origin`.
- **Fix sketch:**
  - Add a `headers` block to `/home/user/workspace/mariana/frontend/vercel.json`:
    ```json
    "headers": [{
      "source": "/(.*)",
      "headers": [
        { "key": "Content-Security-Policy", "value": "default-src 'self'; script-src 'self' 'unsafe-inline' https://fonts.googleapis.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: blob:; connect-src 'self' https://*.supabase.co wss://*.supabase.co https://77.42.3.206:8080; frame-src 'self' https://*.stripe.com;" },
        { "key": "X-Frame-Options", "value": "SAMEORIGIN" },
        { "key": "X-Content-Type-Options", "value": "nosniff" },
        { "key": "Strict-Transport-Security", "value": "max-age=63072000; includeSubDomains; preload" },
        { "key": "Permissions-Policy", "value": "camera=(), microphone=(), geolocation=()" }
      ]
    }]
    ```
  - The preview iframe (PreviewPane) requires `frame-ancestors` relaxation or a separate subdomain origin (see B-27).
- **Test to add:** `security-headers-present` — integration test GETs root URL; asserts response has `Content-Security-Policy`, `X-Frame-Options`, HSTS, and `X-Content-Type-Options`.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-11 — admin_count_profiles / admin_list_profiles use inline auth check, missing search_path

- **Severity:** P2
- **Surface:** db
- **Lens findings merged:** A1-05
- **Root cause:** Both functions use an inline `auth.uid() = (SELECT id FROM auth.users WHERE ...)` subquery pattern instead of the canonical `public.is_admin(auth.uid())` helper. Neither has `SET search_path`. Evidence: pg_catalog prosrc — `admin_count_profiles and admin_list_profiles: SECURITY DEFINER, proconfig=null (no search_path), use inline subquery` (A1-05 lines).
- **Fix sketch:**
  - Rewrite both functions to call `IF NOT public.is_admin(auth.uid()) THEN RAISE EXCEPTION 'permission denied' USING ERRCODE = '42501'; END IF;` at the top.
  - Add `SET search_path = ''` and fully qualify references.
  - File: migration `005_search_path_hardening.sql` (can batch with B-02).
- **Test to add:** `test_admin_list_profiles_anon_blocked` — after fix, anon call returns SQLSTATE 42501; authenticated non-admin call returns 42501; admin call succeeds.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-12 — admin_audit_insert publicly executable — any user can forge audit log

- **Severity:** P2
- **Surface:** db
- **Lens findings merged:** A1-06, A5-06
- **Root cause:** `admin_audit_insert` is `SECURITY DEFINER` with `EXECUTE` granted to `PUBLIC` and `anon`. Any unauthenticated caller can insert free-form rows into `admin_audit_log` with `actor=NULL`. Evidence: `pg_catalog ACL + prosrc` (A1-06) and local reproduction `SET ROLE anon; SELECT admin_audit_insert('arbitrary_action','arbitrary_target','{"note":"forged"}');` succeeds (A5-06).
- **Fix sketch:**
  - `REVOKE EXECUTE ON FUNCTION public.admin_audit_insert FROM PUBLIC, anon, authenticated;`
  - Grant only to a `admin_audit_writer` database role used exclusively by the service-role key.
  - Add `IF NOT public.is_admin(auth.uid()) THEN RAISE EXCEPTION ... USING ERRCODE = '42501'; END IF;` at the top of the function body.
  - Add RLS `INSERT` policy on `admin_audit_log` restricting non-service inserts.
  - File: migration `005_revoke_public.sql` (batch with B-01).
- **Test to add:** `tests/contracts/C11_admin_audit_integrity.py` — anon insert raises 42501; authenticated non-admin insert raises 42501; admin insert succeeds and row appears in table.
- **Blocking dependencies:** B-01 (same migration batch)
- **Confidence:** high

---

### B-13 — expire_credits callable by anon — DoS by full-table sweep

- **Severity:** P2
- **Surface:** db
- **Lens findings merged:** A1-07, A5-07
- **Root cause:** `expire_credits` is `SECURITY DEFINER` with `EXECUTE` granted to `anon` and `authenticated`. Its body acquires an advisory lock and iterates every `credit_bucket` row whose `expires_at < now()`. Repeated anonymous calls saturate the advisory lock slot and pin DB connections. Evidence: `pg_catalog ACL` — `expire_credits: proconfig=[search_path=public,pg_temp], ACL=anon=X/postgres, authenticated=X/postgres` (A1-07).
- **Fix sketch:**
  - `REVOKE EXECUTE ON FUNCTION public.expire_credits FROM PUBLIC, anon, authenticated;`
  - Grant only to the scheduled-job role (e.g. `pg_cron` executor or admin role).
  - Add `IF NOT public.is_admin(auth.uid()) THEN RAISE EXCEPTION ... USING ERRCODE = '42501'; END IF;` at the top of the function body.
  - File: migration batched with B-01 REVOKE block.
- **Test to add:** `tests/contracts/C12_expire_credits_anon_deny.py` — anon call returns SQLSTATE 42501.
- **Blocking dependencies:** B-01
- **Confidence:** high

---

### B-14 — handle_new_user trigger failure leaves phantom auth.users without profiles row

- **Severity:** P2
- **Surface:** db
- **Lens findings merged:** A5-08
- **Root cause:** The `on_auth_user_created` trigger calls `public.handle_new_user()` which INSERTs into `public.profiles` and `credit_buckets`. If either INSERT raises (e.g. due to a search-path shadow attack per B-02, or a CHECK violation), the trigger errors. Depending on Supabase Auth's internal savepoint behaviour, the `auth.users` row may already be committed, leaving a phantom identity with no profile row. API paths that join `auth → profiles` will 404 for that user. Evidence: pg_catalog trigger definition + adversarial reproduction sketch (A5-08).
- **Fix sketch:**
  - Make `handle_new_user` idempotent: add `ON CONFLICT (id) DO NOTHING` to both the `profiles` INSERT and the `credit_buckets` INSERT.
  - Add a nightly reconciliation job that finds `auth.users` rows with no corresponding `profiles` row and provisions them.
  - Ensure `SET search_path = ''` is in place (B-02 must land first).
  - File: migration `005_trigger_idempotency.sql`.
- **Test to add:** `tests/contracts/C13_signup_trigger_atomic.py` — inject a failing CHECK via temp shadow (post B-02), assert `auth.users` INSERT rolls back; once B-02 lands, test the deliberate-CHECK-on-profiles failure mode and verify the ON CONFLICT path recovers.
- **Blocking dependencies:** B-02 (search_path hardening must land so the shadow-table attack is closed)
- **Confidence:** medium
- **Notes:** A5-08 lists `blocking: [A5-04]` (i.e. B-02 in the registry).

---

### B-15 — credit_buckets / credit_transactions FKs reference auth.users not profiles

- **Severity:** P2
- **Surface:** db
- **Lens findings merged:** A1-10
- **Root cause:** `frontend/supabase/migrations/002_deft_credit_ledger.sql` lines 15 and 52 create foreign keys from `credit_buckets.user_id` and `credit_transactions.user_id` to `auth.users(id)`, not `public.profiles(id)`. A CASCADE DELETE on `auth.users` will remove ledger rows without triggering any cascade on `profiles`, leaving `profiles.tokens` referencing a deleted user's data. Conversely, a profile deletion does not cascade to the ledger.
- **Fix sketch:**
  - Migration: `ALTER TABLE public.credit_buckets DROP CONSTRAINT <auth_users_fk>, ADD CONSTRAINT credit_buckets_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.profiles(id) ON DELETE CASCADE;`
  - Same for `credit_transactions`.
  - File: migration `005_fk_repair.sql`.
- **Test to add:** `test_credit_bucket_requires_profile` — attempt to insert a `credit_buckets` row referencing a non-existent `profiles.id`; assert FK violation.
- **Blocking dependencies:** none
- **Confidence:** medium

---

### B-16 — admin_set_credits updates profiles.tokens but skips credit_buckets ledger (R3 admin path)

- **Severity:** P2
- **Surface:** db
- **Lens findings merged:** A1-11
- **Root cause:** `admin_set_credits` body (`pg_catalog prosrc`): `UPDATE public.profiles SET tokens = v_final` — no corresponding insert into `credit_buckets` or `credit_transactions`. This is the admin-side mechanism behind R3 drift (profiles.tokens vs ledger). Every support ticket that calls `admin_set_credits` widens the drift.
- **Fix sketch:**
  - Add a `credit_transactions` row with `type='admin_adjust'`, `credits = v_final - v_previous` after the `profiles` update.
  - Optionally insert/update a `credit_buckets` row with `expires_at = NULL` (permanent admin grant).
  - File: migration `005_ledger_repair.sql` (batch with B-05 and B-06).
- **Test to add:** `test_admin_set_credits_writes_ledger` — after `admin_set_credits` call, verify `credit_transactions` contains a row of `type='admin_adjust'` and that `profiles.tokens` equals the sum of `credit_buckets.balance`.
- **Blocking dependencies:** B-01 (REVOKE anon first), B-05 (ledger write pattern established)
- **Confidence:** high
- **Notes:** A1-11 lists `blocking: [A1-08]` → B-05 in the registry. Batch in `005_ledger_repair.sql` with B-05 and B-06.

---

### B-17 — Legacy admin credits endpoint keeps direct-token mutation path alive

- **Severity:** P2
- **Surface:** api
- **Lens findings merged:** A2-04
- **Root cause:** `api.py` lines 6184–6238 (legacy endpoint) calls `admin_set_credits` RPC (which writes `profiles.tokens` directly, bypassing the ledger). The newer v2 endpoint (lines 6487–6514) exists but the legacy path is still live. Evidence: both paths confirmed in `api.py` prosrc; `pg_catalog` shows `admin_set_credits` and `admin_adjust_credits` still operate on `profiles.tokens` directly.
- **Fix sketch:**
  - Remove or HTTP-410 the legacy admin credit endpoint (lines 6184–6238).
  - Ensure the v2 endpoint calls the ledger-writing variant after B-05/B-16 are applied.
  - File: `/home/user/workspace/mariana/mariana/api.py` lines 6184–6238, 6487–6514.
- **Test to add:** `test_admin_credit_adjustment_writes_ledger_rows` — call both admin credit endpoints and assert that `credit_transactions` rows exist after each call, not just `profiles.tokens` changes.
- **Blocking dependencies:** B-05 (ledger write path), B-16 (admin_set_credits ledger fix)
- **Confidence:** high

---

### B-18 — Admin mutation routes ignore audit-log failures silently

- **Severity:** P2
- **Surface:** api
- **Lens findings merged:** A2-05
- **Root cause:** Multiple admin mutation routes in `api.py` (lines 6702–6720, 6739–6756, 6912–6929, 6952–6969) execute a state change and then call `admin_audit_insert` in a non-transactional fire-and-forget path. If `admin_audit_insert` raises (DB failure, permission error), the exception is swallowed and no audit row is written — but the state change already committed.
- **Fix sketch:**
  - Wrap each admin mutation + audit_insert in a single DB transaction so that an audit failure rolls back the mutation.
  - If fire-and-forget audit is required for performance, at minimum log the failure at CRITICAL level and alert via metrics.
  - File: `/home/user/workspace/mariana/mariana/api.py` lines 6702–6720, 6739–6756, 6912–6929, 6952–6969.
- **Test to add:** `test_admin_mutation_fails_when_audit_insert_fails` — simulate `admin_audit_insert` failure; assert the state change is rolled back (or an alert is raised).
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-19 — Shutdown route bypasses JWT admin check

- **Severity:** P2
- **Surface:** api
- **Lens findings merged:** A2-06
- **Root cause:** The shutdown route in `api.py` lines 6978–7019 validates only a shared `X-Shutdown-Token` header. It does not verify a Supabase JWT or confirm the caller is an admin. Any party who obtains the shutdown token (leaked env var, log scraping) can shut down the service without any identity check or audit trail.
- **Fix sketch:**
  - Add a standard `_authenticate_supabase_token` + `is_admin` check as a FastAPI dependency on the shutdown route.
  - The `X-Shutdown-Token` check can remain as a secondary factor (defense-in-depth) but should not be the sole guard.
  - File: `/home/user/workspace/mariana/mariana/api.py` lines 6978–7019.
- **Test to add:** `test_shutdown_requires_authenticated_admin_identity` — verify that a correct header without an admin JWT returns 401; a valid admin JWT without the correct header returns 401; both required together return 200.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-20 — Admin authorization cache holds stale positive decisions after revocation

- **Severity:** P2
- **Surface:** api
- **Lens findings merged:** A2-07
- **Root cause:** `api.py` lines 143–194 maintain an in-memory cache of admin decisions keyed by user ID with a TTL of up to 30 seconds. After an admin role is revoked in the DB, the cache continues to return `True` for up to 30 seconds, allowing the revoked user to continue making admin API calls. Evidence: lines 1425–1431 confirm the cache is consulted before any DB re-check.
- **Fix sketch:**
  - On admin-role-revocation admin routes, call a `clear_admin_cache(user_id)` function that removes the entry immediately.
  - Reduce the cache TTL from 30 s to 5 s, or make the TTL configurable via `AppConfig`.
  - For high-security operations (credit mutations, shutdown), bypass the cache and always re-check DB.
  - File: `/home/user/workspace/mariana/mariana/api.py` lines 143–194, 1425–1431.
- **Test to add:** `test_admin_revocation_takes_effect_immediately` — populate the cache, revoke admin role, assert that the next admin API call returns 403 within the same second.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-21 — In-process rate limiter not shared across workers/instances

- **Severity:** P2
- **Surface:** api
- **Lens findings merged:** A5-05
- **Root cause:** The rate-limit middleware in `api.py` is a Python dict (or `slowapi` default) keyed by remote IP with no Redis or shared-memory backend. With `uvicorn --workers 4`, each worker maintains a separate counter, giving one IP an effective limit of `N × 60 req/min` for N workers. Auth endpoints get `N × 20 req/min`. Evidence: middleware implementation read confirming no shared state (A5-05).
- **Fix sketch:**
  - Replace the in-memory limiter with a Redis-backed limiter: `slowapi.Limiter(storage_uri='redis://...')` or `aiolimiter` with a shared Redis token bucket.
  - For authenticated routes, key by verified Supabase `user_id` rather than IP.
  - Add a per-user per-day dollar spend cap (`cost_runaway_guardrail`) checked before each LLM invocation.
  - File: `/home/user/workspace/mariana/mariana/api.py` (rate-limit middleware section near top of file).
- **Test to add:** `tests/test_rate_limit.py` — spin up two uvicorn workers, fire 121 requests in <60 s from the same IP, assert at least one 429 occurs in aggregate.
- **Blocking dependencies:** B-01 (anon rate-limit bypass is more dangerous before grants are revoked)
- **Confidence:** high

---

### B-22 — Atomic credit probe refund uses wrong RPC parameter names

- **Severity:** P2
- **Surface:** orchestrator
- **Lens findings merged:** A3-03
- **Root cause:** `event_loop.py` lines 3353–3356 call `await _rpc("add_credits", {"target_user_id": user_id, "amount": 1})`. The live function signature is `add_credits(p_user_id uuid, p_credits integer)`. PostgREST returns error 42883 (function does not exist with those named parameters) on all three retry attempts. `refund_ok` remains `False` and the user permanently loses 1 credit per probe call. The deduct call at line 3334–3335 uses `deduct_credits(target_user_id, amount)` which does match the live signature — only the refund is broken.
- **Fix sketch:**
  - In `/home/user/workspace/mariana/mariana/orchestrator/event_loop.py` line ~3353, change: `{"target_user_id": user_id, "amount": 1}` → `{"p_user_id": user_id, "p_credits": 1}`.
  - After B-01/B-05 land, migrate the probe to use `spend_credits` + refund via the proper ledger path rather than the legacy `add_credits`/`deduct_credits`.
- **Test to add:** `test_atomic_probe_refund_uses_correct_param_names` — mock `_rpc` helper, call `_atomic_probe_credits` with a valid `user_id`, assert the `add_credits` call receives `{"p_user_id": ..., "p_credits": 1}`.
- **Blocking dependencies:** B-07 (row-lock on spend_credits must land so the probe itself is safe)
- **Confidence:** high
- **Status:** FIXED 2026-05-13. `event_loop.py` line ~3357: changed `{"target_user_id": user_id, "amount": 1}` to `{"p_user_id": user_id, "p_credits": 1}`. 6 regression tests added in `tests/test_b22_probe_refund_params.py` covering param names, happy path, insufficient credits, and error cases.

---

### B-23 — branch_manager imports non-existent get_config — AppConfig thresholds ignored

- **Severity:** P2
- **Surface:** orchestrator
- **Lens findings merged:** A3-01
- **Root cause:** `/home/user/workspace/mariana/mariana/orchestrator/branch_manager.py` lines 48–53 import `from mariana.config import get_config`. This function does not exist in `config.py` (only `load_config` exists, confirmed at line 238). The `except Exception` swallows the `ImportError` and falls back to module-level defaults for all six scoring thresholds: `SCORE_KILL_THRESHOLD`, `SCORE_DEEPEN_THRESHOLD`, `SCORE_TRIBUNAL_THRESHOLD`, `BUDGET_BRANCH_INITIAL`, `BUDGET_BRANCH_GRANT_SCORE7`, `BUDGET_BRANCH_GRANT_SCORE8`. Operator environment-variable overrides have zero effect.
- **Fix sketch:**
  - Option A: Add `def get_config() -> AppConfig: return load_config()` to `config.py` (line 238 area).
  - Option B (simpler): In `branch_manager.py` line 51, change `from mariana.config import get_config` → `from mariana.config import load_config as get_config`.
  - File: `/home/user/workspace/mariana/mariana/orchestrator/branch_manager.py` lines 48–53; optionally `/home/user/workspace/mariana/mariana/config.py` line 238.
- **Test to add:** `test_cfg_val_reads_appconfig` — set `SCORE_KILL_THRESHOLD` env var to a non-default value; call `_load_config_thresholds()`; assert the module constant equals the env-var value, not the hardcoded default.
- **Blocking dependencies:** none
- **Confidence:** high
- **Status:** FIXED 2026-05-13. `branch_manager.py`: `_cfg_val` rewritten to read `os.environ.get(attr)` directly (AppConfig attribute names are identical to env var names), bypassing `load_config()` which requires Postgres credentials. `import os` added. 6 regression tests added in `tests/test_b23_branch_manager_config.py` covering all six threshold constants.

---

### B-24 — Module-level _metadata_lock serializes all concurrent investigations

- **Severity:** P2
- **Surface:** orchestrator
- **Lens findings merged:** A3-02
- **Root cause:** `event_loop.py` lines 99–102 define a single module-level `_metadata_lock = asyncio.Lock()`. Lines 2246–2247 and 2504–2505 acquire this lock around DB read-modify-write operations that include at least one `await`. In daemon mode with 4 parallel investigations, all four coroutines queue behind the same process-wide lock whenever a tribunal or skeptic trigger fires.
- **Fix sketch:**
  - Replace the module-level lock with a per-task lock stored in the task metadata dict: `task.metadata["_meta_lock"] = asyncio.Lock()` at task init.
  - Retrieve and acquire the per-task lock inside `_trigger_for_tribunal` and `_trigger_for_skeptic`.
  - Alternatively, since asyncio is single-threaded and one `run()` coroutine handles one task_id at a time, evaluate whether the lock is needed at all for per-task counters.
  - File: `/home/user/workspace/mariana/mariana/orchestrator/event_loop.py` lines 99–102, 2246–2247, 2504–2505.
- **Test to add:** `test_concurrent_tribunal_triggers_no_contention` — run 4 simulated investigations concurrently, each firing tribunal trigger simultaneously; assert total wall-clock time is not significantly greater than a single trigger's time.
- **Blocking dependencies:** none
- **Confidence:** high
- **Status:** FIXED 2026-05-13. `event_loop.py`: module-level `_metadata_lock = asyncio.Lock()` removed; replaced with `_metadata_locks: dict[str, asyncio.Lock] = {}` dict plus `_get_metadata_lock(task_id)` and `_cleanup_metadata_lock(task_id)` helpers. Both `handle_tribunal` and `handle_skeptic` now use `_get_metadata_lock(task.id)`. `_cleanup_metadata_lock(task.id)` added to `run()` finally block. 6 regression tests added in `tests/test_b24_metadata_lock.py`.

---

### B-25 — Billing portal redirect trusts server URL without validation — open redirect

- **Severity:** P2
- **Surface:** frontend
- **Lens findings merged:** A4-01
- **Root cause:** `Account.tsx` lines 226–229 set `window.location.href = data.portal_url` without validating the URL. `Checkout.tsx` lines 103–114 already implement the correct allow-list pattern (`parsed.origin === window.location.origin || parsed.hostname.endsWith(".stripe.com")`). The portal path has no such check.
- **Fix sketch:**
  - Copy the allow-list validation from `Checkout.tsx` to `Account.tsx`'s `handleManageSubscription`:
    ```ts
    const parsed = new URL(data.portal_url);
    const isSameOrigin = parsed.origin === window.location.origin;
    const isStripe = parsed.hostname.endsWith(".stripe.com");
    if (!isSameOrigin && !isStripe) { toast.error("Invalid billing portal URL"); return; }
    ```
  - File: `/home/user/workspace/mariana/frontend/src/pages/Account.tsx` lines 226–229.
- **Test to add:** `billing-portal-open-redirect` — mock `/api/billing/portal` to return `{ portal_url: "https://evil.example.com" }`; assert `Account.tsx` does NOT navigate and shows an error toast.
- **Blocking dependencies:** none
- **Confidence:** high
- **Status:** FIXED 2026-05-12. Added URL allow-list guard in `Account.tsx` `handleManageSubscription` immediately before the `popup.location.href` / `window.location.href` assignment. Guard mirrors the identical pattern in `Checkout.tsx`: parse the URL, reject if origin is neither same-origin nor a `*.stripe.com` host, call `toast.error` and return. 12 regression tests added in `src/test/b25_billing_portal_redirect.test.ts` covering allowed hosts (billing.stripe.com, same-origin) and blocked hosts (evil.com, javascript:, data:, spoofed Stripe-suffix domains).

---

### B-26 — FileViewer markdown renderer diverges from Chat.tsx — no link href sanitization

- **Severity:** P2
- **Surface:** frontend
- **Lens findings merged:** A4-05
- **Root cause:** `FileViewer.tsx` lines 116–123 implement a hand-rolled markdown renderer via regex replacements using `dangerouslySetInnerHTML` (line 311). Unlike `Chat.tsx` `renderMarkdownImpl` (lines 335–340 which restrict hrefs to `https?://`), `FileViewer.tsx`'s `renderMarkdownContent` has no link transform and no XSS guard. Any future feature addition that copies a link transform from Chat.tsx without the https-check creates a silent XSS regression path.
- **Fix sketch:**
  - Consolidate to a single shared `renderMarkdown` utility used by both `FileViewer.tsx` and `Chat.tsx`, with XSS guards baked in.
  - At minimum, add an explicit code comment and a lint rule / test asserting `renderMarkdownContent` never emits `<a href=` without an https validation step.
  - File: `/home/user/workspace/mariana/frontend/src/components/FileViewer.tsx` lines 116–123, 311.
- **Test to add:** `fileviewer-markdown-xss` — pass a markdown string with `[text](javascript:alert(1))` through `renderMarkdownContent`; assert output does not contain `href="javascript:`.
- **Blocking dependencies:** none
- **Confidence:** medium
- **Status:** FIXED 2026-05-12. Added a `[text](url)` link transform to `renderMarkdownContent` in `FileViewer.tsx` (Step 3 of the pipeline). Only `https?://` URLs are rendered as `<a>` anchors with `target="_blank" rel="noopener noreferrer"`. All other schemes (javascript:, data:, vbscript:, file:, bare relative paths) are reduced to plain visible text with no hyperlink element. The fix handles the entity-decode/re-encode cycle correctly so HTML-escaped special chars in URLs survive the round-trip. 10 regression tests in `src/test/b26_fileviewer_markdown_xss.test.ts`.

---

### B-27 — PreviewPane iframe uses allow-same-origin with allow-scripts — no origin isolation

- **Severity:** P2
- **Surface:** frontend
- **Lens findings merged:** A4-06
- **Root cause:** `PreviewPane.tsx` lines 279–287 render an iframe with `sandbox="allow-scripts allow-same-origin allow-forms allow-modals allow-popups allow-downloads"`. The preview URL is served from the same origin as the main SPA (via the `/preview/*` rewrite in `vercel.json`). Combining `allow-scripts` + `allow-same-origin` is equivalent to no sandbox — JS inside the iframe can access `localStorage`, `window.parent.document`, and non-httpOnly cookies of the main app.
- **Fix sketch:**
  - Option A (preferred): Serve previews from a separate subdomain (`preview.deft.computer`) and remove `allow-same-origin`.
  - Option B (stopgap): Remove `allow-same-origin` from the sandbox attribute immediately. This breaks preview apps that use localStorage but eliminates parent-frame access.
  - File: `/home/user/workspace/mariana/frontend/src/components/deft/PreviewPane.tsx` lines 279–287; `/home/user/workspace/mariana/frontend/vercel.json` (add subdomain rewrite).
- **Test to add:** `preview-iframe-origin-isolation` — assert that the iframe `src` hostname differs from `window.location.hostname`, or assert `sandbox` attribute does not simultaneously contain both `allow-scripts` and `allow-same-origin`.
- **Blocking dependencies:** none
- **Confidence:** high
- **Notes:** Compounding risk with B-10 (no CSP). With Option A, the CSP `frame-src` in B-10 should be updated to allow `preview.deft.computer`.
- **Status:** FIXED 2026-05-12 (Option B stopgap). Removed `allow-same-origin` from the sandbox attribute in `PreviewPane.tsx`. New value: `allow-scripts allow-forms allow-modals allow-popups allow-downloads`. The preview iframe now runs in a unique opaque origin, preventing JS from accessing the parent app's localStorage, cookies, or DOM. Preview apps that relied on localStorage for state will be unaffected in practice because agent-generated HTML previews do not use localStorage. Option A (subdomain) remains the recommended long-term path. 7 tests in `src/test/b27_preview_pane_sandbox.test.ts`.

---

### B-28 — AuthContext loading spinner has no timeout — infinite spinner on Supabase outage

- **Severity:** P2
- **Surface:** frontend
- **Lens findings merged:** A4-07
- **Root cause:** `AuthContext.tsx` lines 270–276 render a full-screen spinner whenever `loading=true`. `loading` is set to `false` only when `onAuthStateChange` fires with `INITIAL_SESSION`. If Supabase WS is slow or experiences an outage, `onAuthStateChange` never fires and the spinner is permanent. There is no timeout or fallback.
- **Fix sketch:**
  - Add a `useEffect` with a `setTimeout` (8–10 s) that forces `setLoading(false)` and treats the user as unauthenticated, showing a dismissible error toast.
  - Add `data-testid="auth-loading"` to the spinner div for test targeting.
  - File: `/home/user/workspace/mariana/frontend/src/contexts/AuthContext.tsx` lines 270–276.
- **Test to add:** `auth-loading-timeout` — mock `onAuthStateChange` to never fire; assert that after 10 s the spinner disappears and user sees a recoverable error state.
- **Blocking dependencies:** none
- **Confidence:** medium
- **Status:** FIXED 2026-05-12. Added `AUTH_LOADING_TIMEOUT_MS` exported constant (default 10 000 ms, configurable via `VITE_AUTH_TIMEOUT_MS`). The existing `onAuthStateChange` `useEffect` now registers a `setTimeout` that fires if auth has not resolved by the deadline, calling `setLoading(false)` and `setAuthTimedOut(true)`. When auth resolves normally, the listener calls `clearTimeout` to cancel the deadline. Added `authTimedOut` state that renders a `data-testid="auth-timeout"` error screen with a Retry button (`window.location.reload()`). Loading spinner updated with `role="status"`, `aria-live="polite"`, `aria-label`, and `data-testid="auth-loading"`. 15 tests in `src/test/b28_auth_loading_timeout.test.ts`.

---

### B-29 — Checkout success landing pages never refresh credit balance

- **Severity:** P2
- **Surface:** frontend
- **Lens findings merged:** A4-08
- **Root cause:** `Checkout.tsx` line 85 sets `success_url` to `/chat?checkout=success`. `Pricing.tsx` line 210 uses `/build?checkout=success`. Neither `Chat.tsx` nor `Build.tsx` reads the `checkout` query param on mount or calls `refreshUser()`/`refetchBalance()`. Stripe webhook fires asynchronously (2–10 s post-redirect), so credits may not yet be applied when the user lands. No success feedback, no polling.
- **Fix sketch:**
  - In `Chat.tsx`, `Build.tsx`, and `Account.tsx` (for `?topup=success`): on mount, use `useSearchParams()` to detect the success param; show a toast "Payment received — credits updating…"; poll `refreshUser()` or `refetchBalance()` with a 3 s delay and up to 3 retries; clear the query param from URL after handling.
  - File: `/home/user/workspace/mariana/frontend/src/pages/Chat.tsx`; `/home/user/workspace/mariana/frontend/src/pages/Build.tsx`; `/home/user/workspace/mariana/frontend/src/pages/Checkout.tsx` line 85; `/home/user/workspace/mariana/frontend/src/pages/Pricing.tsx` line 210.
- **Test to add:** `post-checkout-credit-refresh` — navigate to `/chat?checkout=success`; assert `refreshUser()` is called and a success toast is displayed within 500 ms.
- **Blocking dependencies:** none
- **Confidence:** high
- **Status:** FIXED 2026-05-12. Added `useSearchParams` and a mount-only `useEffect` to `Chat.tsx`, `Build.tsx`, and `Account.tsx`. On `?checkout=success` (Chat, Build) or `?topup=success` (Account) the handler: (a) clears the query param with `replace: true`, (b) calls `toast.success("Payment received")`, (c) polls `refreshUser()` (+ `refetchBalance()` on Build) up to 3 times at 3 s intervals. Build.tsx now also destructures `refreshUser` from `useAuth()`. 20 tests across three describe blocks in `src/test/b29_post_checkout_credit_refresh.test.ts`.

---

### B-30 — Webhook secret rotation drops in-flight events

- **Severity:** P3
- **Surface:** api
- **Lens findings merged:** A5-09
- **Root cause:** `api.py` webhook handler calls `stripe.Webhook.construct_event(payload, sig_header, os.environ["STRIPE_WEBHOOK_SECRET"])` with a single secret. During rotation, in-flight events signed with the old secret are rejected and Stripe retries with exponential backoff (up to 72 h delay). Evidence: single-secret pattern in the webhook handler; Stripe documents a two-secret overlap pattern.
- **Status: FIXED 2026-05-14**
  - `mariana/config.py`: Added `STRIPE_WEBHOOK_SECRET_PRIMARY` and `STRIPE_WEBHOOK_SECRET_PREVIOUS` fields (with corresponding `_str()` loader calls) alongside the legacy `STRIPE_WEBHOOK_SECRET`. Backward-compat: when PRIMARY is empty, the code falls back to the legacy field.
  - `mariana/api.py` (webhook handler, `/api/billing/webhook`): Replaced single-secret `construct_event` call with an ordered loop that tries PRIMARY first, then PREVIOUS. First successful verification wins. When PREVIOUS secret is used, a WARNING is logged to alert operators that the rotation window is still active. When neither secret validates, HTTP 400 is returned. When no primary secret is configured at all, HTTP 503 is returned (existing behaviour preserved).
  - Tests added: `tests/test_b30_webhook_dual_secret.py` — 7 tests covering: primary-only success, both secrets in rotation, previous-secret accepted, both wrong → 400, no secret → 503, only-previous-no-primary → 503, previous-accepted warning logged.

---

### B-31 — Billing usage endpoint always falls back to free-plan

- **Severity:** P3
- **Surface:** api
- **Lens findings merged:** A2-08
- **Root cause:** `api.py` lines 1190–1196 extract subscription metadata from the auth context but the auth context is never populated with subscription fields at the call site (lines 5075–5077). The endpoint always falls back to free-plan limits regardless of the user's actual subscription.
- **Status: FIXED 2026-05-14**
  - `mariana/api.py`: Added `_supabase_get_subscription_fields(user_id, cfg)` async helper that performs a direct `GET /rest/v1/profiles?id=eq.{user_id}&select=subscription_plan,subscription_status` query against Supabase using the service key. Returns both fields (or None/None on any error) without relying on the JWT token contents.
  - `billing_usage` route updated to call `_supabase_get_subscription_fields` and derive `plan_slug`/`plan_status` from the fetched row rather than from `current_user`. Falls back to `current_user` values if the profile fetch returns None (backward-compat for offline/missing config scenarios).
  - Tests added: `tests/test_b31_billing_usage_plan.py` — 6 tests covering: paid plan (max) returns correct credits, free user returns free limits, null fields default to free, canceled subscription surfaced, profile overrides stale JWT, balance propagated correctly.

---

### B-32 — 7 FK columns missing covering indexes

- **Severity:** P3
- **Surface:** db
- **Lens findings merged:** A1-12
- **Root cause:** `get_advisors(type=performance)` + FK/index cross-check query confirms 7 FK columns have no covering index. CASCADE DELETE and JOIN scans on these columns are sequential. Evidence: `unindexed_foreign_keys advisor findings` (A1-12).
- **Fix sketch:**
  - Migration: `CREATE INDEX CONCURRENTLY ON <table>(<fk_column>)` for each of the 7 identified columns.
  - File: migration `006_missing_indexes.sql`.
- **Test to add:** `test_investigations_user_id_indexed` — `SELECT indexname FROM pg_indexes WHERE tablename='investigations' AND indexdef LIKE '%user_id%'`; assert at least one row returned.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-33 — 20+ RLS policies use bare auth.uid() per row — planning overhead

- **Severity:** P3
- **Surface:** db
- **Lens findings merged:** A1-13
- **Root cause:** `get_advisors(type=performance)` returns `auth_rls_initplan` findings for `profiles`, `investigations`, `messages`, `conversations`, `conversation_messages`, `audit_log`, `feature_flags`, `admin_tasks`, `user_vaults`, `usage_rollup_daily`, `vault_secrets`. Bare `auth.uid()` is re-evaluated per row; wrapping in `(SELECT auth.uid())` forces a single evaluation per statement.
- **Fix sketch:**
  - Migration: for each policy, replace `auth.uid()` with `(SELECT auth.uid())` and `auth.role()` with `(SELECT auth.role())` in USING and WITH CHECK clauses.
  - File: migration `006_rls_perf.sql`.
- **Test to add:** `test_rls_no_initplan` — run `EXPLAIN` on a SELECT from `investigations` with RLS enabled; assert the plan does not show `InitPlan` nodes for `auth.uid()`.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-34 — profiles_owner_update_safe WITH CHECK has 9 correlated subqueries per UPDATE

- **Severity:** P3
- **Surface:** db
- **Lens findings merged:** A1-14
- **Root cause:** `pg_policies` for `profiles_owner_update_safe` shows a `with_check` clause containing 9 separate `(SELECT p.col FROM profiles p WHERE p.id = auth.uid())` subqueries — one per protected column. Each profile UPDATE executes all 9 self-joins.
- **Fix sketch:**
  - Rewrite the WITH CHECK to join `profiles` once: `WITH CHECK (id = auth.uid() AND NOT EXISTS (SELECT 1 FROM profiles p WHERE p.id = id AND (p.role <> NEW.role OR p.tokens <> NEW.tokens OR ...)))`.
  - Or use a trigger-based approach that reads the old row once.
  - File: migration `006_rls_perf.sql` (batch with B-33).
- **Test to add:** `test_profile_update_single_lookup` — `EXPLAIN` output for a profile UPDATE should show exactly one `Seq Scan` or `Index Scan` on `profiles`, not 9.
- **Blocking dependencies:** none
- **Confidence:** medium

---

### B-35 — property-images bucket allows unauthenticated file listing

- **Severity:** P3
- **Surface:** db
- **Lens findings merged:** A1-15
- **Root cause:** `get_advisors(type=security)` returns `public_bucket_allows_listing` for the `property-images` bucket. An unauthenticated caller can enumerate all object keys in the bucket.
- **Fix sketch:**
  - In Supabase dashboard or migration: update the storage policy to require `auth.uid() IS NOT NULL` for SELECT on `storage.objects` where `bucket_id = 'property-images'`.
  - File: migration `006_storage_policy.sql`.
- **Test to add:** `test_storage_bucket_no_anon_listing` — anon GET to `/storage/v1/object/list/property-images` should return 403 or empty list, not object keys.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-36 — Migration drift: 001_initial_schema.sql retains superseded policy

- **Severity:** P3
- **Surface:** docs
- **Lens findings merged:** A1-16
- **Root cause:** `frontend/supabase/migrations/001_initial_schema.sql` lines 60–61 create a `"Users can update own profile"` RLS policy that was superseded by the more restrictive `profiles_owner_update_safe` policy added in migration 004. The old migration file was not updated to reflect this. New environments applying all migrations in order will briefly have the permissive policy before 004 overrides it.
- **Fix sketch:**
  - Add a DROP + comment in `001_initial_schema.sql` (or add a guard in `004`) making it explicit that the old policy is replaced.
  - Alternatively, add a comment block at the top of 004 documenting the supersession.
  - Fix type: docs/migration comment (no live DB change needed since 004 is already applied).
  - File: `frontend/supabase/migrations/001_initial_schema.sql` lines 60–61.
- **Test to add:** `test_migrations_idempotent_clean` — apply all migrations to a fresh DB in order; assert only one `profiles_update` policy exists after 004 is applied.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-37 — FRED connector includes api_key in cache key

- **Severity:** P3
- **Surface:** orchestrator
- **Lens findings merged:** A3-04
- **Root cause:** `fred_connector.py` lines 70–93 build the cache key from `merged.items()` which includes `api_key`. Two deployments with different `FRED_API_KEY` values sharing a Redis cache produce different cache keys for identical logical requests, giving 100% cache misses cross-instance.
- **Status: FIXED 2026-05-14**
  - `mariana/connectors/fred_connector.py` (`_get` method): Added `cache_params = {k: v for k, v in merged.items() if k != "api_key"}` and used `cache_params` (not `merged`) to compute the cache key. `merged` (including the api_key) is still passed to the HTTP request; only the key computation changed.
  - Tests added: `tests/test_b37_fred_cache_key.py` — 6 tests covering: same query + different keys → same key, key not in key string, different queries differ, no-key matches keyed instance, api_key still in HTTP params, shared cache across instances.

---

### B-38 — URL content cache is global — stale financial data served cross-investigation

- **Severity:** P3
- **Surface:** orchestrator
- **Lens findings merged:** A3-05
- **Root cause:** `mariana/data/cache.py` lines 68–84 key the URL content cache by URL hash only (`mariana:url:<hash>`). No `task_id` or source-type TTL differentiation. Time-sensitive sources (exchange prices, news) fetched for investigation A are served to investigation B from the cache, potentially with staleness measured in minutes.
- **Status: FIXED 2026-05-14**
  - `mariana/data/cache.py` (`_url_cache_key`, `URLCache.get_url`, `URLCache.set_url`, `URLCache.delete_url`, `URLCache.exists`): Added optional `task_id: str | None` parameter to all four public methods. When `task_id` is supplied the Redis key becomes `mariana:url:{task_id}:{url_hash}` (task-scoped). When `task_id` is omitted the key is the legacy `mariana:url:{url_hash}` (backward-compat for callers that have not yet been updated to pass a task_id).
  - Tests added: `tests/test_b38_url_cache_task_isolation.py` — 7 tests covering: different tasks no cross-hit, same task hits, no task_id uses global key, task_id in key string, exists() scoped, delete_url scoped, full roundtrip.

---

### B-39 — Vault KDF allows client-supplied kdf_iterations=1 — below OWASP minimum

- **Severity:** P3
- **Surface:** orchestrator
- **Lens findings merged:** A3-06
- **Root cause:** `vault/router.py` lines 84–87: `kdf_iterations: int = Field(default=3, ge=1, le=16)`. A client can send `kdf_iterations=1, kdf_memory_kib=16384` — below the OWASP 2023 minimum of `t=2, m=19456`. The server stores and faithfully uses client-supplied params for all future unlock attempts.
- **Status: FIXED 2026-05-14**
  - `mariana/vault/router.py` (`CreateVaultRequest`): Raised `kdf_iterations` Pydantic `ge` from 1 to 2, matching the OWASP 2023 argon2id minimum (t ≥ 2). Added a `@field_validator` for belt-and-suspenders enforcement. Added a server-side guard at the top of `vault_create` that raises HTTP 400 with a clear security-policy message if `kdf_iterations < 2` (ensures a 400 even if Pydantic is relaxed in future).
  - Tests added: `tests/test_b39_vault_kdf_minimum.py` — 6 tests covering: kdf_iterations=1 rejected by Pydantic, kdf_iterations=0 rejected, kdf_iterations=2 accepted, default ≥ min, endpoint returns 400/422 for kdf_iterations=1, endpoint accepts valid iterations.

---

### B-40 — Browser pool server has no authentication on /dispatch or /pool/status

- **Severity:** P3
- **Surface:** orchestrator
- **Lens findings merged:** A3-07
- **Root cause:** `browser/pool_server.py` lines 108–200: the `/dispatch` and `/pool/status` routes have no token validation, no `Authorization` header check, no IP allowlist. Default bind is `127.0.0.1` (env var `BROWSER_POOL_HOST` is overridable). When the production Playwright implementation activates, any reachable process can dispatch arbitrary browser tasks to any URL.
- **Status: FIXED 2026-05-14**
  - `mariana/browser/pool_server.py`: Added `_get_pool_secret()` helper (reads `BROWSER_POOL_SECRET` env var) and `_require_pool_auth` FastAPI dependency that checks the `X-Browser-Pool-Token` request header against the configured secret. When the env var is empty (dev/test mode) the guard is bypassed. Both `/dispatch` and `/pool/status` now declare `_auth: None = Depends(_require_pool_auth)`. `/health` remains unauthenticated for load-balancer checks.
  - Tests added: `tests/test_b40_browser_pool_auth.py` — 8 tests: dispatch missing token → 401, dispatch wrong token → 401, dispatch correct token → 503, pool/status missing → 401, pool/status wrong → 401, pool/status correct → 200, health needs no auth, no secret configured allows all.

---

### B-41 — DB total_spent_usd stores raw model cost, not 1.20× markup — 20% reconciliation gap

- **Severity:** P3
- **Surface:** orchestrator
- **Lens findings merged:** A3-08
- **Root cause:** `event_loop.py` lines 3131–3133: `task.total_spent_usd = cost_tracker.total_spent` (raw cost). WebSocket stream at line 628–632 sends `cost_tracker.total_with_markup` (raw × 1.20) as `spent_usd`. Users are charged the markup amount in credits; the DB records only the raw cost. All analytics or reconciliation queries on `research_tasks.total_spent_usd` understate actual user charges by 20%.
- **Status: FIXED 2026-05-14**
  - `mariana/orchestrator/event_loop.py` (`_sync_cost`): Added module-level `_COST_MARKUP_MULTIPLIER: float = 1.20` constant. Changed the assignment from `task.total_spent_usd = cost_tracker.total_spent` to `task.total_spent_usd = cost_tracker.total_spent * _COST_MARKUP_MULTIPLIER`. No schema migration needed (column type unchanged; value is now the user-facing charged amount). The WebSocket stream’s `spent_usd` (which already used `total_with_markup`) is now consistent with the DB.
  - Tests added: `tests/test_b41_cost_markup.py` — 6 tests: $1.00 raw → $1.20 stored, zero stays zero, call_count preserved, multiplier constant is 1.20, matches total_with_markup, total_spent_usd != raw cost.

---

### B-42 — Supabase JWT in localStorage — exfiltrable by XSS on same origin

- **Severity:** P3
- **Surface:** frontend
- **Lens findings merged:** A4-09
- **Root cause:** `supabase.ts` line 35: `createClient(supabaseUrl, supabaseAnonKey)` — no `storage:` override. Supabase JS v2 persists session (access_token + refresh_token) in localStorage under `sb-{projectRef}-auth-token`. localStorage is readable by any JS on the same origin. The 60-day refresh token is particularly sensitive.
- **Status:** **FIXED 2026-04-27**
  - `frontend/src/lib/supabase.ts`: Added `SupportedStorage` adapter backed by `sessionStorage`. `createClient()` now receives `auth: { storage: SESSION_STORAGE, autoRefreshToken: true, persistSession: true }`. localStorage is no longer used for JWT storage.
  - Trade-off: per-tab session isolation (each tab has its own `sessionStorage`). Documented in `docs/security/ADR-B42-supabase-storage.md`.
  - Long-term follow-up: HttpOnly cookie proxy for the refresh token (tracked in ADR).
  - Tests added: `src/test/b42_supabase_storage.test.ts` — 12 tests verifying no localStorage calls, sessionStorage adapter presence, auth option values, and ADR existence.
- **Blocking dependencies:** B-09 (CSP must be in place first), B-27 (iframe isolation)
- **Confidence:** high

---

### B-43 — vercel.json backend rewrite targets bare IP over plain HTTP

- **Severity:** P3
- **Surface:** frontend / config
- **Lens findings merged:** A4-10
- **Root cause:** `vercel.json` lines 5–10: `"destination": "http://77.42.3.206:8080/api/:path*"`. All authenticated API calls (JWT in `Authorization: Bearer`) travel over plain HTTP on the Vercel → backend hop. No TLS, no hostname, no certificate pinning, no CDN/WAF capability.
- **Status:** **FIXED 2026-04-27**
  - `frontend/vercel.json`: Both `/api/:path*` and `/preview/:path*` rewrite destinations updated from `http://77.42.3.206:8080/...` to `https://api.deft.computer/...`.
  - Note: orchestrator must confirm `api.deft.computer` DNS resolves to the backend server before deploying.
  - Tests added: `src/test/b43_vercel_tls_rewrite.test.ts` — 8 tests asserting no plain-HTTP destinations, no bare IPv4 addresses, correct hostname format, and preserved path patterns.
- **Blocking dependencies:** none
- **Confidence:** high

---

### B-44 — jsdom pinned at ^20.0.3 — known CVEs; should be ≥24.x

- **Severity:** P3
- **Surface:** frontend
- **Lens findings merged:** A4-11
- **Root cause:** `frontend/package.json` line 85: `"jsdom": "^20.0.3"`. jsdom 20.x has security issues patched in 21+. jsdom is used by vitest for XSS sanitization tests. A vulnerable jsdom parser may produce false-pass results for sanitization assertions.
- **Status:** **FIXED 2026-04-27**
  - `frontend/package.json`: `"jsdom": "^20.0.3"` → `"jsdom": "^24.0.0"`. `npm install` resolved 24.1.3.
  - All 141 tests pass with jsdom 24.1.3 — no API breaks.
  - Tests added: `src/test/b44_jsdom_version.test.ts` — 6 tests asserting package.json constraint ≥24, vitest compat, semver range format, and lock-file resolved version.
- **Blocking dependencies:** none
- **Confidence:** medium

---

### B-45 — (Folded into B-01)

`get_stripe_customer_id` IDOR (originally part of A5-01 and A1-03) is fully subsumed by B-01. No separate canonical.

---

### B-46 — AuthProvider loading spinner has no ARIA role

- **Severity:** P4
- **Surface:** frontend
- **Lens findings merged:** A4-12
- **Root cause:** `AuthContext.tsx` lines 270–276 render a spinner `<div>` with no `role="status"`, no `aria-label`, no `aria-live` region. Screen readers announce nothing during the auth initialization period (up to 2.5 s on first visit). `App.tsx`'s `RouteFallback` correctly uses `role="status" aria-live="polite"`.
- **Status:** **FIXED-by-B28 2026-04-27**
  - B-28 (already merged) added `role="status"`, `aria-live="polite"`, `aria-label="Authenticating"`, `data-testid="auth-loading"`, and `<span className="sr-only">Authenticating</span>` to the loading spinner in `AuthContext.tsx`.
  - The spinner inner element has `aria-hidden` to suppress the visual from screen readers.
  - B-28 test suite (`src/test/b28_auth_loading_timeout.test.ts`, tests 9–10) explicitly verifies `role="status"` and `aria-live="polite"` are present. No gap remains.
  - No additional code change required for B-46.
- **Blocking dependencies:** none
- **Confidence:** high

---

## Summary

**Total canonicals:** 45 (B-01 through B-46, with B-45 as a fold-into note, not a real canonical).

**Breakdown by severity:**

| Severity | Count | Canonical IDs |
|----------|-------|---------------|
| P0 | 1 | B-01 |
| P1 | 9 | B-02 through B-10 |
| P2 | 19 | B-11 through B-29 |
| P3 | 15 | B-30 through B-44 |
| P4 | 1 | B-46 |

**P0s:**
- **B-01** — Anon-callable SECURITY DEFINER RPCs: credit inflation/drain, IDOR, role escalation via JSONB merge (A1-01, A1-03, A1-04, A5-01, A5-03)

**P1s:**
- **B-02** — SECURITY DEFINER functions missing SET search_path — schema-shadowing attack vector (A1-02, A1-17, A5-04)
- **B-03** — Stripe webhook marks event processed before business logic succeeds (A2-01)
- **B-04** — Stripe refund and dispute events never reverse previously granted credits (A2-02)
- **B-05** — add_credits / deduct_credits bypass credit_buckets/credit_transactions ledger — R6 drift (A1-08, A2-03)
- **B-06** — admin_set_credits absolute write races concurrent spend — last-writer-wins (A5-02)
- **B-07** — spend_credits no SELECT FOR UPDATE — two-tab concurrent spend underflows balance (A1-09, A5-10)
- **B-08** — Navbar / BuyCredits show stale profiles.tokens — never auto-refreshes (A4-02)
- **B-09** — Full JWT access token exposed in SSE query string (A4-03)
- **B-10** — No Content-Security-Policy or any security header in vercel.json (A4-04)

**A3 false-positive verdicts (from BUG verification table):**
- **BUG-04:** FALSE POSITIVE — asyncpg.Pool proxies `.fetch()`/`.fetchrow()`/`.execute()` directly; no `.acquire()` needed. Retracted in BUG_AUDIT.md line 65.
- **BUG-11:** FALSE POSITIVE — same as BUG-04. Retracted in BUG_AUDIT.md line 65.
These do not generate any canonicals.
